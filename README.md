# Catching Game

This project uses Vite which requires at least Node 20. It won't with lower Node versions.

# How to play

The game will work locally after running `npm install` and then starting it with `npm start`. For production build meant to be deployed `npm run build` should be ran and after doing it the game will be avaiable to check in prod mode with `npm run preview`.
